# Preset: embedding
# Model:  nomic-embed-text (primary RAG embeddings)
# Use:    Embed documents and queries for RAG retrieval.
#         CPU-only by default — fast enough for embedding workloads.

OLLAMA_MODEL=nomic-embed-text
