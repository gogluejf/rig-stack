# Preset: starvector — raster to SVG vector generation
# Model:  starvector/starvector-8b-im2svg
# Use:    Convert raster images (PNG/JPG) to clean SVG vector output.
#         Runs via ComfyUI's custom StarVector node.
# HF:     https://huggingface.co/starvector/starvector-8b-im2svg

COMFYUI_MODEL=starvector-8b-im2svg
COMFYUI_MODEL_PATH=/models/starvector/starvector-8b-im2svg
COMFYUI_PREVIEW_METHOD=auto
COMFYUI_LISTEN=0.0.0.0
COMFYUI_PORT=8188
