# rig-stack model registry
# Format (tab-separated, 3 columns):
#   source    local-dest    description
#
# - source    : HuggingFace repo ID (e.g. org/model) or ollama/<model-name>
# - local-dest: path under $MODELS_ROOT — used as registry key and for filesystem ops
#               ollama models use "ollama/<model-name>" (managed by Ollama, not MODELS_ROOT)
# - description: shown in `rig models` list
#
# This file is written by `rig models pull` and `init-models.sh`.
# Do not edit manually unless you know what you are doing.
